<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/src/output.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>WriteVibe</title>
</head>
<body class=" overflow-hidden w-full min-h-screen text-white bg-[url('/images/download.svg')] bg-cover bg-right-top h-screen">

<header>
    <nav class="flex justify-between sm:flex-row sm:items-center sm:justify-between sticky top-0 z-10 p-5">
        <h1 class="text-2xl sm:text-3xl">WriteVibe</h1>
        <div class="flex gap-3">
            <a href="/register" class="bg-green-500 rounded-md px-4 py-2 text-sm sm:text-base  sm:mt-0 lg:mt-2">
                Register
            </a>
            <a href="/login" class="bg-green-500 rounded-md px-4 py-2 text-sm sm:text-base  sm:mt-0 lg:mt-2">
                Log-In
            </a>
        </div>
    </nav>
    <hr class="border-t border-gray-600 mx-5 sm:mt-[-34px] sm:ml-40 sm:mr-52 lg:mt-[-2.5rem] ">
</header>

<div class="p-10">  
      <h1 class="text-3xl"><span  class="text-3xl text-blue-300">Writes</span>~</h1>
</div>
<div class="posts">
    <div class="flex flex-wrap w-full -mx-0 sm:mx-2">
        <% post.reverse().forEach((post) => { %>
            <div class="post mb-4 sm:m-2 border rounded-lg h-36 w-full sm:w-[48%] lg:w-[32%] flex flex-col">
                <div class="flex justify-between p-2">
                    <div class="flex items-center">
                        <img class="w-[32px] h-[32px] rounded-full object-cover mr-1" src="/images/uploads/<%= post.user.dp %>" alt="helo">
                        <h5 class="text-blue-500">@<%= post.user.username %></h5>
                    </div>
                    <% 
                      let date = new Date(post.date);
                      let formattedDate = date.getDate().toString().padStart(2, '0') + ' ' +
                                          date.toLocaleString('en-US', { month: 'short' }).toUpperCase() + ', ' +
                                          date.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit', hour12: true });
                    %>
                    <h5 class="text-gray-500 text-xs sm:text-sm">
                        <%= formattedDate %>
                    </h5>
                </div>
                <div class="overflow-auto flex-grow p-2">
                    <p class="tracking-tighter">
                        <%= post.content %>
                    </p>
                </div>
            </div>
        <% }) %>
    </div>
</div>
</body>
</html>
